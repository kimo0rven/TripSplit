export interface FriendProfile {
  id: string;
  fullname: string;
  email: string;
  photoURL?: string;
  currency?: string;
}

export interface UserSearchResult {
  id: string;
  fullname: string;
  email: string;
  photoURL?: string;
}